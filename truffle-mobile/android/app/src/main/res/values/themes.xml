<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Truffle Theme - Darkroom Design System
        Project Truffle - Mobile Companion (Section 5.3)
        
        Background: #0A0A0A (Pure Black OLED optimization)
        Surface: #141414 (Elevated cards)
        Primary: #FFB800 (Amber/Warning)
        Secondary: #00D4AA (Success/Confirmation)
    -->

    <!-- Base Theme -->
    <style name="Theme.Truffle" parent="android:Theme.Material.NoActionBar">
        <!-- Colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/background</item>
        <item name="android:colorAccent">@color/primary</item>
        
        <!-- Window -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        
        <!-- Text -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

    <!-- Transparent Theme for Share Activity -->
    <style name="Theme.Truffle.Transparent" parent="Theme.Truffle">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>

    <!-- Dialog Theme -->
    <style name="Theme.Truffle.Dialog" parent="android:Theme.Material.Dialog">
        <item name="android:colorBackground">@color/surface</item>
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>
</resources>
